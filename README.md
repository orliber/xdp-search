# xdp-search

Python experiments comparing Weighted A*, XDP, and XUP best-first search.

Repository: https://github.com/orliber/xdp-search

## Collaborator

Yair Lavy  
GitHub: [yairlavy](https://github.com/yairlavy)  
Pronouns: he/him

## Files

- `algorithms.py` - shared best-first search core and priority functions.
- `domains.py` - 15-puzzle and random obstacle grid domains.
- `experiments.py` - benchmark runner, CSV output, and printed tables.
- `visualize.py` - matplotlib plots of average expansions by weight.

## Install

```bash
python3 -m pip install --user -r requirements.txt
```

## Run

```bash
python3 experiments.py
```

This writes:

- `results/results_raw.csv`
- `results/results_summary.csv`

To generate plots:

```bash
pip install -r requirements.txt
python3 visualize.py
```

For a quick smoke test:

```bash
python3 experiments.py --output-dir /private/tmp/xdp-check --grid-width 8 --grid-height 8 --grid-instances 1 --puzzle-instances 1 --puzzle-scramble-moves 8
```

Default experiments use random seed `20260602`, weights `1.5, 2.0, 3.0, 10.0`,
and grid obstacle densities `10%, 20%, 30%, 40%` with 50 random instances per
density. The default 15-puzzle benchmark uses 10 solvable instances generated by
15 random scramble moves; pass `--puzzle-instances` and `--puzzle-scramble-moves`
for larger puzzle runs.

## Outputs

- CSV summaries are written to `results/`.
- Expansion plots are written to `plots/`.
